server:
  port: 8080

spring:
  main:
    allow-bean-definition-overriding: true
    web-application-type: reactive
  application:
    name: api-gateway
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI:https://auth.agentbanking.com}
          jwk-set-uri: ${JWT_JWK_SET_URI:https://auth.agentbanking.com/.well-known/jwks.json}
  cloud:
    gateway:
      routes:
        # Rules Service
        - id: rules-service
          uri: http://localhost:8081
          predicates:
            - Path=/internal/fees/**, /internal/check-velocity, /internal/limits/**
          filters:
            - RewritePath=/internal/(?<segment>.*), /internal/${segment}
        
        # Ledger Service
        - id: ledger-service
          uri: http://localhost:8082
          predicates:
            - Path=/internal/debit, /internal/credit, /internal/balance/**, /internal/reverse/**
          filters:
            - RewritePath=/internal/(?<segment>.*), /internal/${segment}
        
        # Onboarding Service
        - id: onboarding-service
          uri: http://localhost:8083
          predicates:
            - Path=/internal/verify-mykad, /internal/biometric
          filters:
            - RewritePath=/internal/(?<segment>.*), /internal/${segment}
        
        # Switch Adapter Service
        - id: switch-adapter-service
          uri: http://localhost:8084
          predicates:
            - Path=/internal/auth, /internal/reversal, /internal/duitnow
          filters:
            - RewritePath=/internal/(?<segment>.*), /internal/${segment}
        
        # Biller Service
        - id: biller-service
          uri: http://localhost:8085
          predicates:
            - Path=/internal/validate-ref, /internal/pay-bill, /internal/topup
          filters:
            - RewritePath=/internal/(?<segment>.*), /internal/${segment}
        
        # External API routes (through JWT filter)
        - id: external-withdrawal
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/withdrawal
          filters:
            - JwtAuthFilter
            - RewritePath=/api/v1/(?<segment>.*), /internal/debit
        
        - id: external-deposit
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/deposit
          filters:
            - JwtAuthFilter
            - RewritePath=/api/v1/(?<segment>.*), /internal/credit
        
        - id: external-balance-inquiry
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/balance-inquiry, /api/v1/agent/balance
          filters:
            - JwtAuthFilter
            - RewritePath=/api/v1/(?<segment>.*), /internal/balance/${segment}
        
        - id: external-kyc-verify
          uri: http://localhost:8083
          predicates:
            - Path=/api/v1/kyc/verify
          filters:
            - JwtAuthFilter
            - RewritePath=/api/v1/kyc/verify, /internal/verify-mykad
        
        - id: external-kyc-biometric
          uri: http://localhost:8083
          predicates:
            - Path=/api/v1/kyc/biometric
          filters:
            - JwtAuthFilter
            - RewritePath=/api/v1/kyc/biometric, /internal/biometric
        
        # Backoffice routes
        - id: backoffice-agents
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/backoffice/agents/**
          filters:
            - JwtAuthFilter
        
        - id: backoffice-transactions
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/backoffice/transactions
          filters:
            - JwtAuthFilter
        
        - id: backoffice-settlement
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/backoffice/settlement/**
          filters:
            - JwtAuthFilter
        
        - id: backoffice-kyc-review
          uri: http://localhost:8083
          predicates:
            - Path=/api/v1/backoffice/kyc/review-queue
          filters:
            - JwtAuthFilter

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always
